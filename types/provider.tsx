/* eslint-disable no-unused-vars */

export enum Provider {
  OpenAI = "openai",
  Anthropic = "anthropic",
  Google = "google",
  OpenRouter = "openrouter"
}

export enum Reasoning {
  High = "high",
  Medium = "medium",
  Low = "low"
}
